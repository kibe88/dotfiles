# http://stackoverflow.com/questions/22697440/cc-failed-with-exit-status-1-error-when-install-python-library
export CFLAGS=-Qunused-arguments
export CPPFLAGS=-Qunused-arguments

LUNCHY_BASH_COMPLETION=$(dirname `gem which lunchy`)/../extras/lunchy-completion.bash
#TODO: check running shell and source accordingly
LUNCHY_ZSH_COMPLETION=$(dirname `gem which lunchy`)/../extras/lunchy-completion.zsh
if [ -f $LUNCHY_ZSH_COMPLETION ]; then
    . $LUNCHY_ZSH_COMPLETION
fi
